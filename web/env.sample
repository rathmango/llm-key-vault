# Copy these to your hosting environment variables

# Public (browser)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server only
SUPABASE_SERVICE_ROLE_KEY=

# 32-byte base64 key for AES-256-GCM
# Generate with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
LLMKV_ENCRYPTION_KEY=

# Web search (server only)
# This project uses OpenAI Responses API built-in `web_search` tool.
# No additional 3rd-party API key is required.

# YouTube Data API v3 (server only)
# Used for Home > YouTube recommendations (thumbnails/titles only).
# Create in Google Cloud Console and set one of:
# - YOUTUBE_DATA_API_KEY (recommended)
# - YOUTUBE_API_KEY (supported for compatibility)
YOUTUBE_DATA_API_KEY=

